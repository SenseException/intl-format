language: php

php:
  - '7.1'
  - '7.2'
  - nightly

install:
  - composer install --dev --prefer-source

script:
  - vendor/bin/phpunit
  - vendor/bin/phpstan analyse

matrix:
  fast_finish: true
  allow_failures:
    - php: nightly